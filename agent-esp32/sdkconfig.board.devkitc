# Board: DevKitC (2x USB-C, 8MB PSRAM)
# Console on UART (COM port) — frees USB PHY for OTG Host mode
CONFIG_SPIRAM=y
CONFIG_SPIRAM_MODE_OCT=y
CONFIG_SPIRAM_USE_MALLOC=y
CONFIG_ESP_CONSOLE_UART_DEFAULT=y
# USB Host (for USB mass storage via OTG port)
CONFIG_USB_HOST_CONTROL_TRANSFER_MAX_SIZE=1024
